@model SSWeb.Models.Worksheet

@{
    ViewBag.Title = "CreateWorksheet";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Name Worksheet (Step 2 of 2)</h2>
<hr />
        @using (Html.BeginForm("EditDemo","Worksheets", FormMethod.Post, new { enctype = "multipart/form-data" }))
        {
         
        @Html.ValidationSummary(true)
        <fieldset>
        @Html.HiddenFor(model => model.WorksheetId)

                    <div class="editor-label">
            @Html.LabelFor(model => model.Name)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.Name)
            @Html.ValidationMessageFor(model => model.Name)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.Description)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.Description)
            @Html.ValidationMessageFor(model => model.Description)
        </div>
            </fieldset>
        <button class="btn" type="submit" value="CreateWorksheet">Save</button>            
            <!--
            
             <div class="control-group">
                <label class="control-label" for="inputName">Name</label>
                <div class="controls">
                <input type="text" id=inputName placeholder="Test Workbook"/>
                </div>
             </div>
            
             <div class="control-group">
                <label class="control-label" for="inputDescription">Description</label>
                <div class="controls">
                <textarea rows="3" id=inputDescription>Type a description here...</textarea>
                </div>
             </div>
            -->
            
        }